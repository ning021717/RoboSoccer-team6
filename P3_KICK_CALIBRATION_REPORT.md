# P3 足端接触标定：真实 Webots 证据报告

日期：2026-09-15。此报告只陈述本仓库 `evidence/` 下真实 Webots 运行产生的日志；不把动作调用次数当作踢球成功。

## 新增的可观测链

`决策动作 → 控制器足尖碰撞传感器/足底力 → 髋膝踝位置传感器 → Supervisor 球位移/速度 → 评分事件`

Supervisor 只写评估遥测；球员控制器不会读取其中的足端或球真值来决定运动。每个球员现已启用双相机；在 `ROBOCUP_BALL_SOURCE=camera` 时，控制器只使用相机估计，Supervisor 真值仅作带标签评估。

## 已完成的真实运行

| 运行 | 条件 | 观测 | 结论 |
|---|---|---|---|
| `p3_kick_calibration_pilot_3x3_r2` | 9 个真实 6s 落点格；无球速注入 | 9/9 有 supervisor 球遥测；其中 `f=0.18m, l=-0.06m` 记录 0.13470m 位移，但当时未有控制器足端遥测 | 不能归因为踢球 |
| `p3_kick_contact_confirmation_f018_lm006_r2` | 同候选格，14s，原生足尖/足底/关节记录，录屏 | 94 次动作请求；球位移 0.35002m、峰值 0.14389m/s；碰撞发生在 `turnRight10` 而不是 `rightShoot` | **证据不足，不能计为射门成功** |
| `p3_kick_contact_confirmation_f018_lm006_r3_t25` | 将标定专用近球对准容差由 15° 调至 25° | Webots 视频编码在 180s 运行器期限内未正常结束 | 不纳入数据集；保留原始目录供排障 |

`r2` 的严格分析器结果为：`no_toe_contact_during_kick_motion`。因此现阶段不存在可信的“射门成功率”“传球成功率”或 4v4 进球统计。

## 可复现视频和截图

`evidence/p3_kick_contact_confirmation_f018_lm006_r2/complete_kick_flow.mp4` 是正常录制的 14s 球场画面（18,901,894 bytes）；同目录 `frames/` 保存 4s 接近/对准、8s 摆腿、12s 随动截图。视频用于排障和过程核对，**不是**完成进球的证据。

## 通过的右脚击球基线

在收缩后的真实 3×3 走廊扫描（前向 `0.14/0.18/0.22 m`、横向
`-0.06/-0.04/-0.02 m`、对准容差 25°）中，有 2/9 格满足完整 P3
物理门。最稳定的候选是 `forward=0.14 m`、`lateral=-0.04 m`：随后三个
独立 Webots 进程复测均为 `physical_kick_success`，每次记录到足尖触发、
`0.48711 m` 球位移和 `0.69896 m/s` 峰值球速，且没有球速注入。

这证明的是该固定姿态下的**可重复物理击球基线**，不是完整比赛进球率，
也不是多 seed 统计：运行是确定性的，尚未引入受控随机化。

## 阶段门与下一动作

该固定姿态门现已通过，因此可以开始单个 5 分钟固定条件基线。进入多 seed
参数实验和 15 分钟 4v4 前，仍须建立受控随机初始条件和每角色可配置参数。
物理击球门的判据为：

1. 射门或传球动作窗口内足尖传感器触发；
2. 同一窗口后球位移至少 0.10m，峰值球速至少 0.10m/s；
3. 原始 JSONL、动作日志和视频（如启用）存在；
4. 连续复测而不是单次偶然碰撞。

下一轮应扫描 `kick_alignment_tolerance_deg × strike_standoff_m`，并把实际脚尖/球相对位姿加入嵌入式 NAO 资产或以原生接触传感器替代。任何用 Supervisor 人为设球速的方式都不属于 P3 验证。
